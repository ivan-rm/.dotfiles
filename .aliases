# File system
alias config='cd $HOME/.config'
alias dotfiles='cd $HOME/.dotfiles'

alias ff="fzf --preview 'batcat --style=numbers --color=always {}'"
alias fdt="fd . '.config/' | fzf --preview 'batcat --style=numbers --color=always {}'"

# Tools
#alias code='code --enable-features=UseOzonePlatform --enable-features=WaylandWindowDecorations --ozone-platform-hint=auto'
alias n='nvim'
alias vim='nvim'
alias t='tmux'
alias g='git'
alias bat='batcat'
alias lzg='lazygit'
#alias lzd='lazydocker'
alias fd='fdfind'

# Tool's keybindings
alias cr='cargo run'
#alias python='python3'
#alias pip='pip3'
alias activate='source .venv/bin/activate'
alias nve='python -m venv .venv/'

# Managing dotfiles with a bare repo 
alias dotf='/usr/bin/git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME'

# View lines around code changes with proper syntax highlighting
batdiff() {
    git diff --name-only --relative --diff-filter=d -z | xargs -0 batcat --diff
}

# Compression
compress() { tar -czf "${1%/}.tar.gz" "${1%/}"; }
alias decompress="tar -xzf"

# Convert webm files generated by the Gnome screenshot video recorder to mp4s that are more compatible
webm2mp4() {
  input_file="$1"
  output_file="${input_file%.webm}.mp4"
  ffmpeg -i "$input_file" -c:v libx264 -preset slow -crf 22 -c:a aac -b:a 192k "$output_file"
}
